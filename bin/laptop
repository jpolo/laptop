#!/usr/bin/env bash
# shellcheck source=/dev/null

###
### laptop — A tool for maintaining your laptop (cleaning, upgrading, etc)
###
### Usage: laptop [options] <subcommand>
###
### Options:
###   -h,--help: Show this message.
###   -y,--yes:  Skip confirmation prompts and assume yes
###
### Subcommands:

SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
ROOT_DIR=$(dirname "$SCRIPT_DIR")

LAPTOP_HOME=${LAPTOP_HOME:-"$ROOT_DIR"}
LAPTOP_CONFIG_ZSH_FILE=${LAPTOP_CONFIG_ZSH_FILE:-}
LAPTOP_LIB_DIR=${LAPTOP_LIB_DIR:-"$LAPTOP_HOME/lib"}
source "$LAPTOP_LIB_DIR/env.sh"
source "$LAPTOP_LIB_DIR/command/cleanup.sh"
source "$LAPTOP_LIB_DIR/command/config.sh"
source "$LAPTOP_LIB_DIR/command/configure.sh"
source "$LAPTOP_LIB_DIR/command/upgrade.sh"
source "$LAPTOP_LIB_DIR/command/version.sh"

###   cleanup: empty cache and prune unused resources
###   config zsh [--view] [--edit] view or edit the configuration file
###   configure: install and setup every tool for the current profile for this device
###   upgrade: upgrade all tools and packages
###   help: show this message
laptop_command__help() {
  sed -rn 's/^### ?//p' "$0"
}

###
### Environment variables:
###   LAPTOP_HOME: Local folder of installation of the laptop executable and library
###   LAPTOP_CONFIG_ZSH_FILE: Path to the shell personal configuration file (default: $XDG_CONFIG_HOME/zsh/personal.sh)
###   LAPTOP_GIT_REMOTE: Git repository location used as zsh plugin. It can be :
###     - A github repository name (ex: jpolo/laptop)
###     - A full git url (ex: https://github.com/jpolo/laptop.git)
###   LAPTOP_PROFILE: current profile to use for the configure sub command
###     the profile source must be available at `$LAPTOP_HOME/profile/$LAPTOP_PROFILE/profile.sh`

# Parse global options and subcommand
LAPTOP_YES=false
LAPTOP_HELP=false
LAPTOP_SUBCOMMAND=""
LAPTOP_ARGS=()
LAPTOP_BOOTSTRAP=${LAPTOP_BOOTSTRAP:-false}

while [[ $# -gt 0 ]]; do
  case $1 in
    -y|--yes)
      # shellcheck disable=SC2034
      LAPTOP_YES=true
      shift
      ;;
    -h|--help)
      # shellcheck disable=SC2034
      LAPTOP_HELP=true
      shift
      ;;
    -v|--version)
      # shellcheck disable=SC2034
      laptop_command__version
      exit 0
      ;;
    *)
      if [ -z "$LAPTOP_SUBCOMMAND" ]; then
        LAPTOP_SUBCOMMAND=$1
      else
        LAPTOP_ARGS+=("$1")
      fi
      shift
      ;;
  esac
done

# Run the program
case $LAPTOP_SUBCOMMAND in
  "" | "help")
    laptop_command__help
    ;;
  "version")
    laptop_command__version
    ;;
  *)
    laptop_command__${LAPTOP_SUBCOMMAND} "${LAPTOP_ARGS[@]}"
    if [ $? = 127 ]; then
      echo "Error: '$LAPTOP_SUBCOMMAND' is not a known subcommand." >&2
      echo "       Run '$SCRIPT_NAME --help' for a list of known subcommands." >&2
      exit 1
    fi
    ;;
esac
